- url: https://{baseurl}
  description: InfluxDB 3 Core API URL
  variables:
    baseurl:
      enum:
        - 'localhost:8080'
      default: 'localhost:8080'
      description: InfluxDB 3 Core URL
