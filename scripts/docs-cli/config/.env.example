# InfluxData Documentation Tooling Configuration
# Copy this file to .env in the repository root and fill in your values
# NEVER commit .env file - it's in .gitignore

# ==============================================================================
# Repository Paths (for local development)
# ==============================================================================
# Optional - CLI will search common locations if not set
# Only configure these if your repos are in non-standard locations

# InfluxData docs-v2 repository (public)
# DOCS_V2_PATH=~/Documents/github/influxdata/docs-v2

# InfluxDB Core repository paths (public)
# INFLUXDB_CORE_PATH=~/Documents/github/influxdata/influxdb

# Telegraf repository (public)
# TELEGRAF_PATH=~/Documents/github/influxdata/telegraf

# ==============================================================================
# Product Access
# ==============================================================================
# Set to true if you have access to audit Enterprise products
# Requires proper GitHub authentication and repository access

# DOCS_ENTERPRISE_ACCESS=false

# ==============================================================================
# Repository URLs (for audit cloning)
# ==============================================================================
# Leave as default to use standard InfluxData repositories
# Only customize if using forks or alternative remotes

# Core product repository URL (public)
# DOCS_CORE_REPO_URL=https://github.com/influxdata/influxdb.git

# Enterprise product repository URL (requires authentication if private)
# DOCS_ENTERPRISE_REPO_URL=

# Telegraf repository URL (public)
# DOCS_TELEGRAF_REPO_URL=https://github.com/influxdata/telegraf.git

# Documentation repository URL (public)
# DOCS_REPO_URL=https://github.com/influxdata/docs-v2.git

# ==============================================================================
# Worktree Configuration
# ==============================================================================
# Base path for worktree project folders (defaults to ./.worktrees)

# WORKTREE_BASE_PATH=./.worktrees

# ==============================================================================
# GitHub Authentication
# ==============================================================================
# This CLI uses GitHub CLI (gh) for authentication
# No need to configure tokens here - just run: gh auth login
#
# To verify authentication: gh auth status
