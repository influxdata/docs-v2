influxdb:
  default: v4
  cloud: v4

flux:
  default: v4

enterprise_influxdb:
  default: v2

chronograf:
  default: v2

telegraf:
  default: v2

kapacitor:
  default: v2

resources:
  default: v4

# Only used by content/example.md for testing icons
example:
  default: v4